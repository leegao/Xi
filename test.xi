use io

mean(list:int[]):int{
	print("hello world!")
	sum:int = 0 
	i:int = 0 
	while (i < length(list)) 
		sum = sum + list[i]; 
	x(1,2,3,4,5,6,7,8);
	return sum/length(list)
}

x(a:int, b:int, c:int, d:int, e:int, f:int, g:int, h:int){
	a':int[] = "1"
	a' = a' + a';
}